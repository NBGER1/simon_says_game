namespace Infrastructure.Events
{
    public enum EventTypes
    {
        OnRivalTakeDamage,
        OnPlayerTakeDamage,
        OnPlayerAddHealth,
        OnRivalAddHealth,
        OnRivalTurn,
        OnPlayerTurn,
    }
}